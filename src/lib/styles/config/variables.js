import formatVariables from './formatVariables.js';

const variables = {
	font_code: `'Source Code Pro', monospace`,
	font_main: `Montserrat, sans-serif`,
	font_heading: `Rajdhani, sans-serif`,
};

export default formatVariables(variables);
